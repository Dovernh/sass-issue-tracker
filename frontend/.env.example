# The Angular client reads its config from src/environments/*.ts (apiUrl,
# sentryDsn), not from a .env file — there are no frontend secrets to set.
# Point the client at a different backend by editing environment.ts / .prod.ts.
